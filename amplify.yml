version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp *.html robots.txt sitemap.xml manifest.webmanifest sw.js *.txt dist/ || true
        - cp -R css js assets dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
